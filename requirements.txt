# Pinned to what the committed artifacts were generated with.
# See pyproject.toml. Install into .venv:
#   uv venv .venv && uv pip install --python .venv/bin/python -r requirements.txt
oracledb==4.0.2
